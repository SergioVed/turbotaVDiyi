import { StyleSheet, Text, View, Image, FlatList, SafeAreaView } from "react-native";

export const ImportantPage = () => {

    return(
        <SafeAreaView>
            
        </SafeAreaView>
    )
}